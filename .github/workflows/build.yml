name: Build

on:  
#   release:
#     types: [published]
  push:
    branches: [ "master" ]
  # pull_request:
  #   branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --bin wol
    - name: Run tests
      run: cargo test --bin wol
